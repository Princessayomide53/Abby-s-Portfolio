import Home from '@/components/UI/Quickfill/Home';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default page;
