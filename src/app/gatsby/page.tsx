import React from 'react';
import Home from '@/components/UI/Epp/Home';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default page;
