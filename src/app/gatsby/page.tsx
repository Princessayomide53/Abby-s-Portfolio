import React from 'react';
import Gatsby from '@/components/UI/Epp/Gatsby';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Gatsby />
    </div>
  );
};

export default page;
